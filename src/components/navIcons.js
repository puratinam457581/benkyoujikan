import { House, CalendarDays, ChartColumn, Settings, FileText } from 'lucide-react'

// タブ/サイドバー共通のアイコン割り当て。
export const NAV_ICONS = {
  home: House,
  calendar: CalendarDays,
  analysis: ChartColumn,
  export: FileText,
  settings: Settings,
}
