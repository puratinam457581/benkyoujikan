# 勉強時間管理ツール 仕様書

## 1. 目的

日々の勉強時間を可視化し、継続的な学習の振り返りを支援するツール。

**最優先事項(この2点を最重要視して設計・実装すること)**

1. **記録のシームレスさ**:勉強時間の登録を可能な限り手間なく行えること
2. **勉強時間の可視化**:「今までどれくらい勉強したか」「今日はどれくらい勉強したか」を直感的に把握できること

## 2. 実装形態

- PWA(Progressive Web App)として構築し、PC・スマートフォンの両方で利用可能にする
- ホーム画面に追加した状態での利用を前提とする

## 3. 技術スタック

- フロントエンド: React + Tailwind CSS + Vite(vite-plugin-pwa)
- 認証: Firebase Authentication(Googleアカウントログインのみ)
- データベース: Firebase Firestore
- ホスティング: 無料の静的ホスティング(GitHub Pages 等)
- 完全無料での構築を前提とし、Firebase無料枠(Sparkプラン)の範囲内で収まる設計とする

## 4. データ同期方式

- リアルタイム同期(常時接続)は不要
- 記録・編集を保存したタイミングでFirestoreへ書き込む
- アプリ起動時に最新データをFirestoreから取得して画面へ反映する
- オフライン時の記録はFirestoreのオフライン永続化機能を利用し、オンライン復帰時に自動反映する
- 複数端末での同時編集は想定しない(競合解決の複雑な仕組みは不要)

## 5. データ構造

記録は「教科 → 教材 → 活動内容」の3階層タグで管理する。

- 教科(例: 英語、数学)
  - 教材(例: 英語 → みかん、TOEIC公式参考書)
    - 活動内容(例: みかん → リスニング、単語 / TOEIC公式参考書 → 文法、読解)

**登録方法**

- 事前登録は不要。記録画面で新しい教科・教材・活動内容を入力すると自動的に選択肢として保存され、次回以降はタップで選択可能になる(初回のみ手入力、以降はワンタップ選択)
- 一度使った組み合わせは履歴として保持し、記録画面で候補表示する

**記録項目**

- 教科
- 教材
- 活動内容
- 勉強時間(数値入力)
- 日付
- (任意)自由記述メモ

## 6. 記録フロー

記録は「勉強が終わった後にまとめて入力する」方式とする(勉強前後のタイマー起動は必須としない)。

1. 「記録する」ボタンをタップ(画面下部など押しやすい位置に配置)
2. 教科を選択、または新規入力
3. 教材を選択、または新規入力(選択した教科に紐づく候補のみ表示)
4. 活動内容を選択、または新規入力(選択した教材に紐づく候補のみ表示)
5. 勉強時間を入力
6. 保存

**シームレス化のための工夫**

- よく使う「教科・教材・活動内容」の組み合わせをショートカットボタンとして記録画面上部に表示し、1タップで選択完了できるようにする
- 「前回と同じ内容」を1タップで呼び出せるボタンを設置する
- 教材ごとにアイコン・色を設定し、文字よりも視覚的に選べるようにする

## 7. 画面構成

### 7.1 ホーム画面

- 画面上部: 「今日の合計勉強時間」を最も目立つ形で大きく表示
- その下: 今日の円グラフ(教科別割合)
- 「記録する」ボタンを画面下部など押しやすい位置に常時配置
- 総勉強時間、カレンダー、比較機能への導線をタブまたはスクロールで配置

### 7.2 カレンダー画面

- 月表示のカレンダーで、日ごとの合計勉強時間を表示(例: 日付セル内に「3.5h」等)
- 日付タップで、その日の教科別内訳(円グラフ・リスト)を表示

### 7.3 月間サマリー

- 月末(または任意タイミング)に、その月の合計勉強時間・教科別内訳を可視化
- 前月との比較も表示(詳細は8章の比較機能を参照)

### 7.4 総計画面

- これまでの総勉強時間を表示
- 教科・種類ごとの総勉強時間を集計して一覧表示(グラフ+リスト)

## 8. 比較機能

固定パターンのみで実装する(任意期間選択は不要)。

- **日次比較**: 今日 と 昨日
- **週次比較**: 今週 と 先週
- **月次比較**: 今月 と 先月

各比較で以下を表示する。

- 合計勉強時間の増減(時間・パーセンテージ)
- 教科別内訳の増減
- 数値サマリー(例:「今週は先週より3時間20分増加」)
- 期間を並べた棒グラフ

## 9. 未確定・今後の検討事項

- タイマー機能(ストップウォッチ/ポモドーロ)の採否は保留
- 連続記録(ストリーク)表示の採否は保留
- 記録忘れ防止のリマインダー通知の採否は保留
- 上記は必須機能ではないため、中核機能の実装後に追加検討する

## 10. 参考

- 実装方針・技術スタックは既存プロジェクトの[[timetable-app]](大学の時間割管理アプリ)を踏襲する
- データ管理方式のみ、時間割アプリのJSONエクスポート/インポート方式から、Firebase(Google認証+Firestore)によるアカウント同期方式へ変更する
